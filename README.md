# Simple Task Manager

Веб-додаток для управління завданнями з сервіс-орієнтованою архітектурою.

## 🛠 Технології

### Backend
- **Python 3.10+**
- **FastAPI** — REST API фреймворк
- **MySQL** — база даних
- **Pydantic** — валідація даних
- **Uvicorn** — ASGI сервер

### Frontend
- **React 18** + **TypeScript**
- **Vite** — збірка та dev-сервер
- **Tailwind CSS** — стилізація
- **shadcn/ui** — UI компоненти
- **Sonner** — сповіщення

## 📁 Структура проекту

```
Simple Task Manager/
├── back/               # Backend (FastAPI)
│   ├── app.py          # Головний контролер
│   ├── models.py       # Pydantic моделі
│   ├── registry.py     # Service Discovery клієнт
│   ├── config.py       # Конфігурація
│   └── requirements.txt
├── front/              # Frontend (React)
│   ├── src/
│   │   ├── api/        # API клієнт
│   │   ├── components/ # React компоненти
│   │   ├── hooks/      # Custom hooks
│   │   ├── pages/      # Сторінки
│   │   └── types/      # TypeScript типи
│   └── package.json
└── start.bat           # Скрипт запуску (Windows)
```

## 🚀 Запуск

### Швидкий запуск (Windows)
```bash
start.bat
```

### Ручний запуск

#### 1. Backend
```bash
cd back
pip install -r requirements.txt
python app.py
```
Сервер запуститься на `http://127.0.0.1:8000`

#### 2. Frontend
```bash
cd front
npm install
npm run dev
```
Додаток доступний на `http://localhost:8080`

## 📡 API Endpoints

| Метод | Endpoint | Опис |
|-------|----------|------|
| GET | `/` | Health check |
| GET | `/health` | Статус сервісу |
| GET | `/categories` | Список категорій |
| GET | `/tasks` | Список завдань |
| POST | `/tasks` | Створити завдання |
| GET | `/tasks/{id}` | Отримати завдання |
| PATCH | `/tasks/{id}/status` | Оновити статус |

**Swagger документація:** http://127.0.0.1:8000/docs

## ⚙️ Функціональність

- ✅ Створення, перегляд та видалення завдань
- ✅ Категоризація завдань
- ✅ Три рівні пріоритету (Low, Medium, High)
- ✅ Статуси: Pending, Completed, Overdue
- ✅ Автоматичні нагадування (за 30 хв до дедлайну)
- ✅ Звукові сповіщення
- ✅ Адаптивний інтерфейс

## 🔧 Конфігурація

### Backend
Налаштування підключення до БД у `back/config.py` або через Service Registry.

### Frontend
API URL налаштовується у `front/src/api/taskApi.ts`:
```typescript
const API_BASE_URL = 'http://127.0.0.1:8000';
```

## 📋 Вимоги

- Python 3.10+
- Node.js 18+
- MySQL 8.0+
